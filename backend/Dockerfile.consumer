# Dockerfile for notification consumer service
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY consumer ./consumer

ENV NODE_ENV=production
ENV NOTIFY_WS_PORT=4001

EXPOSE 4001

CMD ["node", "consumer/notificationService.js"]
